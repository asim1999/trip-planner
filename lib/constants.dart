import 'package:flutter/material.dart';

const kFormFieldStyle = TextStyle(color: Colors.black);

const kFormMargin = EdgeInsets.symmetric(vertical: 25, horizontal: 10);

const kFormPadding = EdgeInsets.symmetric(vertical: 25, horizontal: 25);

const kFormFieldFill = Color(0xfff8f8f8);
